server.port=8080

spring.data.redis.host=localhost
spring.data.redis.port=6379

rate.limit.max=100
rate.limit.window=60000


##Set the jwt secret key from env variables in your env
jwt.secret=${JWT_SECRET}
jwt.expiration=3600000
